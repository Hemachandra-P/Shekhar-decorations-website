import './globals.css';

export const metadata = {
  title: 'Shekhar Events | Wedding & Event Decoration',
  description: 'Elegant wedding, reception, engagement and event decorations by Shekhar Events.',
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
